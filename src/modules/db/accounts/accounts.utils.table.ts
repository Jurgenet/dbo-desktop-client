import { accountsDto } from '@/modules/db/accounts'

export function getColumns () {
  return [
    // {
    //   name: '_id',
    //   required: true,
    //   label: '_id',
    //   align: 'left',
    //   field: (row: accountsDto.IAccount) => row._id,
    //   format: (val: string) => `${val}`,
    //   sortable: false,
    // },
    {
      name: 'title',
      required: true,
      label: 'Title',
      align: 'left',
      field: (row: accountsDto.IAccount) => row.title,
      format: (val: string) => `${val}`,
      sortable: false,
    },
    {
      name: 'group',
      required: true,
      label: 'Group',
      align: 'left',
      field: (row: accountsDto.IAccount) => row.group,
      format: (val: string) => `${val}`,
      sortable: false,
    },
    {
      name: 'email',
      required: true,
      label: 'Email',
      align: 'left',
      field: (row: accountsDto.IAccount) => row.email,
      format: (val: string) => `${val}`,
      sortable: false,
    },
    {
      name: 'login',
      required: true,
      label: 'Login',
      align: 'left',
      field: (row: accountsDto.IAccount) => row.login,
      format: (val: string) => `${val}`,
      sortable: false,
    },
    {
      name: 'password',
      required: true,
      label: 'Password',
      align: 'left',
      field: (row: accountsDto.IAccount) => row.password,
      format: (val: string) => `${val}`,
      sortable: false,
    },
    // {
    //   name: 'text',
    //   required: true,
    //   label: 'Text',
    //   align: 'left',
    //   field: (row: accountsDto.IAccount) => row.text,
    //   format: (val: string) => `${val}`,
    //   sortable: false,
    // },
    {
      name: 'link',
      required: true,
      label: 'Link',
      align: 'left',
      field: (row: accountsDto.IAccount) => row.link,
      format: (val: string) => `${val}`,
      sortable: false,
    },
    // {
    //   name: 'markers',
    //   required: true,
    //   label: 'Markers',
    //   align: 'left',
    //   field: (row: accountsDto.IAccount) => row.markers,
    //   format: (val: string) => `${val}`,
    //   sortable: false,
    // },
    {
      name: 'actions',
      required: false,
      label: '',
      align: 'left',
      sortable: false,
    },
  ]
}
